/**
 * Renders the nested step tree for a run as an indented vertical
 * timeline. Each node shows: name, duration, color-coded status
 * (success/error), and is clickable to open StepDetailPanel.
 *
 * Implemented in Phase 5.
 */

// TODO(Phase 5): implement <StepTree steps={...} onSelectStep={...} />
export {};
