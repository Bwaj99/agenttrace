/**
 * Side panel shown when a step is selected: pretty-printed,
 * collapsible input/output JSON, token usage, and duration. Hosts
 * the "Replay" button for llm_call steps (see ReplayPanel).
 *
 * Implemented in Phase 5.
 */

// TODO(Phase 5): implement <StepDetailPanel step={...} />
export {};
