/**
 * Cost/latency summary bar shown at the top of a run detail page:
 * total tokens, total cost, total duration, and the slowest step
 * highlighted.
 *
 * Implemented in Phase 5.
 */

// TODO(Phase 5): implement <SummaryBar run={...} />
export {};
