/**
 * Runs list page (route: "/").
 *
 * Planned (Phase 5):
 * - Fetch GET /runs from the backend.
 * - Render a table: name, timestamp, duration, step count, total
 *   cost, status badge (color-coded: running / completed / failed).
 * - Each row links to /runs/[runId] (the run detail/timeline page).
 */

// TODO(Phase 5): implement the runs list page.
export default function RunsListPage() {
  return null;
}
