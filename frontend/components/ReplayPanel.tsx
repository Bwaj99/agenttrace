/**
 * Editable textarea (pre-filled with the original step input) that
 * calls POST /runs/{run_id}/steps/{step_id}/replay and renders a
 * side-by-side diff of the original vs. replayed output. Only shown
 * for llm_call steps.
 *
 * Implemented in Phase 5.
 */

// TODO(Phase 5): implement <ReplayPanel step={...} runId={...} />
export {};
